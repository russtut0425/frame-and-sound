name: Verify and complete album covers

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/fetch-album-covers-pass2.yml

permissions: {}

concurrency:
  group: album-cover-archive
  cancel-in-progress: false

jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Verify and complete cover archive
        id: covers
        continue-on-error: true
        run: node scripts/fetch-covers-pass2.mjs
      - name: Save verified covers and remove this one-time task
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add album-covers.js album-covers.json unmatched-covers.json covers 2>/dev/null || true
          git add -u covers
          git rm .github/workflows/fetch-album-covers-pass2.yml scripts/fetch-covers-pass2.mjs
          git commit -m "Verify and complete album cover art [skip ci]"
          git push
      - name: Report matching failure
        if: steps.covers.outcome == 'failure'
        run: exit 1
