# 帧与声

电影与专辑私人档案的 GitHub Pages 版本。

- 无需构建，直接发布仓库根目录。
- 记录保存在当前浏览器的 `localStorage`。
- 支持新增、编辑、删除、搜索、筛选、排序，以及 JSON 备份导入导出。

## GitHub Pages

仓库 Settings → Pages → Build and deployment，选择 **Deploy from a branch**，分支选择 `main`，目录选择 `/ (root)`。
